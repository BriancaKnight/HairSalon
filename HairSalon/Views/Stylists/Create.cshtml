@{
  Layout = "_Layout";
}

@model HairSalon.Models.Stylist

<h2>Add New Stylist</h2>

@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.StylistName)
  @Html.TextBoxFor(model => model.StylistName, new { required = "required"})
  <input type="submit" value="Add new Stylist" />
}

<div class="navigation">
<div>@Html.ActionLink("Show all Stylist", "Index")</div>
<div>@Html.ActionLink("Return Home", "Index", "Home")</div>
</div>